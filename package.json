{
  "scripts": {
    "prod": "vite build && npx serve -s dist",
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && netlify deploy"
  },
  "type": "module",
  "dependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "phaser": "^3.88.2"
  },
  "devDependencies": {
    "prettier": "^3.5.2",
    "ts-node": "^10.9.2",
    "tsc-watch": "^6.2.1",
    "typescript": "^5.7.3",
    "vite": "^6.2.0"
  }
}
